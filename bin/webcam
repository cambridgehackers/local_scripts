#!/bin/bash
set -e
set -x

B="100K"
F="ogg"
echo " (Currently must press 'play' to get video to display)"
ssh -p 8000 localhost "ffmpeg -b $B -an -f video4linux2 -s 320x240 -r 10 -i /dev/video0 -b $B -f $F - " \
    | /Applications/VLC.app/Contents/MacOS/VLC - --playlist-autostart
